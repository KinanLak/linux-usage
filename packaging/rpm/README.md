# RPM packaging notes

Planned packages:

- `linux-usage-helper`: installs the Rust helper binary and a user service file later
- `gnome-shell-extension-linux-usage`: installs the extension payload under the user or system extension dir

This folder currently documents the target package split but does not yet contain full spec files.
